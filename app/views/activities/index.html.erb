<div class="board">
  <% Status.all.each do |status| %>
    <div class="list">
      <div class="header-list">
      <div class="header-title"><%= status.name%></div>
        <% if status.id == 1 %>
          <div class="header-button"type="button">
            <%= link_to "+", new_activity_path, class: 'header-button-text' %>
          </div>
        <% end %>
      </div>
      <div class="list-body">
        <%= render 'card_index', status_id: status.id, disabled: (status.id == 4) %>
      </div>
    </div>
  <% end %>
</div>

